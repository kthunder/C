#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>
#include <assert.h>

uint32_t u32 = 0;
uint64_t u64 = 0;
uint64_t max = 0;

int main(int argc, char* argv[])
{
	printf("%s", ucCase[0][1]);
	// uint64_t temp = 0;
	// while (u32++ != 0xFFFFFFFF)
	// {
	//     temp = u32 - u64;
	//     max = max > temp ? max : temp;
	// }

	// printf("sizeof(u32 - u64) %X\n",sizeof(u32 - u64));
	// printf("u32 - u64 %X\n",u32 - u64);
	// printf("max %X\n",max);
}